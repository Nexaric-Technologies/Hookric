# Client-side env vars (Vite injects any var starting with VITE_)
#
# Production API base URL — the Cloudflare Worker that owns /r/:id.
# The default is in .env.production; copy this file to .env.local to
# override per-developer.
VITE_HOOKRICK_API=https://hookrick.nexaricdigital.workers.dev
